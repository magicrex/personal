class Solution {
public:
    bool Treeheight(TreeNode* root, int& height){

    }
    bool IsBalanced_Solution(TreeNode* pRoot) {
        if(pRoot==NULL)
            return true;
        int left=0,left=0;
        if(Treeheight(pRoot->right)==false||Treeheight(pRoot->left)==false)
            return false;
        return true;
            
    }

};
